{
  "name": "certchain-caliper-benchmarks",
  "version": "1.0.0",
  "private": true,
  "description": "Hyperledger Caliper performance benchmarks for the CertChain chaincode (Phase 4 evaluation).",
  "scripts": {
    "validate": "caliper launch manager --caliper-workspace . --caliper-networkconfig networks/certchain-network.yaml --caliper-benchconfig benchconfig.yaml --caliper-flow-only-test",
    "benchmark": "caliper launch manager --caliper-workspace . --caliper-networkconfig networks/certchain-network.yaml --caliper-benchconfig benchconfig.yaml --caliper-report-path reports/report.html"
  },
  "dependencies": {
    "@hyperledger/caliper-cli": "0.7.1",
    "@hyperledger/caliper-fabric": "0.7.1",
    "fabric-network": "^2.2.20"
  }
}
